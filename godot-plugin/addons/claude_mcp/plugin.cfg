[plugin]
name="Claude MCP"
description="Exposes a local HTTP server so Claude can read/write scenes, scripts, and assets via the MCP protocol."
author="Claude MCP Server"
version="1.0.0"
script="claude_mcp_plugin.gd"
